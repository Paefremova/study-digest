__pycache__/
*.pyc
.ruff_cache/

# Личные настройки: копируется из config.env.example при установке
config.env

# Локальные токены (rutube refresh/access)
.secrets/

# Снимок состояния сводки (внутри digest)
.state.json
state/
